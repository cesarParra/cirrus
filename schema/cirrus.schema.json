{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/cesarParra/cirrus/main/schema/cirrus.schema.json",
  "title": "cirrus.yaml",
  "description": "Salesforce development automation: the orgs a project creates, the commands it runs, and the flows that sequence them.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "defaultOrg": {
      "description": "The org `cirrus org create` creates when it is not told which one. Names a key under `orgs`.",
      "$ref": "#/$defs/name"
    },
    "orgs": {
      "description": "The scratch orgs this project creates, keyed by the name you refer to them by.",
      "type": "object",
      "propertyNames": { "$ref": "#/$defs/name" },
      "additionalProperties": {
        "$ref": "#/$defs/org"
      }
    },
    "commands": {
      "description": "The commands this project runs, keyed by the name `cirrus run` takes.",
      "type": "object",
      "propertyNames": { "$ref": "#/$defs/name" },
      "additionalProperties": {
        "$ref": "#/$defs/command"
      }
    },
    "flows": {
      "description": "Sequences of steps, run in order, stopping at the first step that fails.",
      "type": "object",
      "propertyNames": { "$ref": "#/$defs/name" },
      "additionalProperties": {
        "$ref": "#/$defs/flow"
      }
    }
  },
  "$defs": {
    "name": {
      "description": "A name is letters, digits, '-' and '_', starting with a letter or a digit, because it is typed on the command line.",
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9_-]*$"
    },
    "org": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "definitionFile"
      ],
      "properties": {
        "definitionFile": {
          "description": "Path to the scratch org definition file, relative to the directory holding cirrus.yaml.",
          "type": "string"
        },
        "duration": {
          "description": "How many days the scratch org lives. Salesforce's own default applies when this is absent.",
          "type": "integer",
          "minimum": 1,
          "maximum": 30
        },
        "alias": {
          "description": "The alias the org is created under. Defaults to the name this org is keyed by.",
          "type": "string"
        },
        "namespace": {
          "description": "Whether the org carries the project's package namespace. Set false for an org standing in for a subscriber's, which does not.",
          "type": "boolean"
        },
        "wait": {
          "description": "Minutes to wait for the org to be created. Salesforce's own default applies without it.",
          "type": "integer",
          "minimum": 1
        }
      }
    },
    "command": {
      "description": "The command line to run, or a mapping carrying it. Commands are not run through a shell: `&&`, pipes and $VARIABLES are arguments rather than syntax.",
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "run": {
              "description": "The command line to run. A command with only `dependsOn` is a name for its prerequisites, and runs nothing itself.",
              "type": "string"
            },
            "description": {
              "description": "What this command is for, shown in `cirrus run --help`.",
              "type": "string"
            },
            "dependsOn": {
              "description": "Commands that must have run before this one. A prerequisite, not a step: each completes before this command starts, and runs once however many times it is named.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "not": {
            "description": "A command with neither runs nothing and names nothing.",
            "properties": {
              "run": false,
              "dependsOn": false
            }
          }
        }
      ]
    },
    "flow": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "steps"
      ],
      "properties": {
        "description": {
          "description": "What this flow is for, shown in `cirrus flow --help`.",
          "type": "string"
        },
        "steps": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/step"
          }
        },
        "dependsOn": {
          "description": "Commands that must have run before the flow's first step: each completes before the first step starts, and runs once however many times it is named.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "step": {
      "description": "A step names its kind with its first key.",
      "type": "object",
      "oneOf": [
        {
          "additionalProperties": false,
          "required": [
            "createScratch"
          ],
          "properties": {
            "createScratch": {
              "description": "The name of an org defined under `orgs`.",
              "type": "string"
            },
            "setDefault": {
              "description": "Set the created org as the CLI's default. True when absent.",
              "type": "boolean"
            }
          }
        },
        {
          "additionalProperties": false,
          "required": [
            "command"
          ],
          "properties": {
            "command": {
              "description": "The name of a command defined under `commands`.",
              "type": "string"
            }
          }
        }
      ]
    }
  }
}
